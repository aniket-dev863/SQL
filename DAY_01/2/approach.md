Final row represents:
...

Tables needed:
...

Intermediate result:
...

Why GROUP BY:
...

Why JOIN/subquery:
...

Final query:
...

Important Points :

1. WHERE -> Filter Rows 2.
2. GroupBy -> Creates one Group per value
3. HAVING -> Filters group
4. Scalar Subquery -> Returns one value
5. Correlated Subquery -> Depends upon the outer row
6. MAX/AVG Give the value not automatically the row that produced it
7. "For each X " think group by X
8. Need the actual row with MAX/MIN → calculate value, then JOIN back
9. Derived Table creates the intermediate result join them with outer table .
